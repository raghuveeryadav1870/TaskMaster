!function (e, t, r) {
    "use strict";
    r(".repeater-default").repeater({
        isFirstItemUndeletable: true,
        show: function () {
            r(this).slideDown(),
            $(this).find('.select2-container').remove();
            var selects = $(this).find('.select2-placeholder');
            $(selects).each(function (i, v) {
                $(this).removeAttr('id');
            });
            $('.select2-placeholder').select2({
                placeholder: '...',
                allowClear: true
            });
            $(selects).each(function (i, v) {
                $(this).val('0').trigger("change");
            });
            $(this).find('.select2-container').css('width', '100%');
        },
        hide: function (e) {
            r(this).slideUp(e)
        },
        ready: function (i) {
        }
    }),
        r(".file-repeater, .contact-repeater").repeater({ isFirstItemUndeletable: true, show: function () { r(this).slideDown() }, hide: function (e) { confirm("Are you sure you want to remove this item?") && r(this).slideUp(e) } })
}(window, document, jQuery);